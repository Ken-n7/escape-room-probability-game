# Audio Assets

The game loads these files through `src/audio/audio-assets.js`.

To change a sound later, replace a file here with the same filename, or edit the
matching `src` value in `src/audio/audio-assets.js`. MP3, OGG, and WAV files are supported.
