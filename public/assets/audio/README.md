# Audio Assets

The game loads these files through `audio-assets.js` in the project root.

To change a sound later, replace a file here with the same filename, or edit the
matching `src` value in `audio-assets.js`. MP3, OGG, and WAV files are supported.
